void main() {
  const multipler = 10;
  const list = [1,2,4];
  final result = list.map((x) => x * multipler);
print(result);
}

// When we declare an anonymous function that 
// uses a variable that are defined outside its own 
//scope then that function is called a CLOSURE.

